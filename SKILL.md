---
name: personal-management-system
description: >
  个人管理系统（Personal Management System）。将十槽分类、命名规范、同步策略应用于文件组织。当用户提到文件分类、整理、命名、该放哪里、
  按个人管理规范、同步策略、大文件处理、目录结构组织等问题时，使用此技能。
  也适用于用户新增内容时不确定归属分类、想检查文件命名是否符合规范、
  需要了解跨平台同步配置（Notion/ima/文件系统）等场景。
  只要涉及个人文件组织整理的意图，就应该触发。
---

# 个人管理系统 — Personal Management System

将用户的十槽分类体系、文件命名规范、同步策略应用于文件组织任务。

## 启动检查

每次被调用时，首先检查 README 和 reference 两文件是否一致：

```bash
diff 个人管理系统/README.md references/personal-management-system.md
```

- 一致 → 继续执行
- 不一致 → 以修改时间较新的为准，`cp` 覆盖旧的一方，并提示用户"已自动同步"
- 某方缺失 → 从另一方 `cp` 恢复

## 核心职责

1. **分类决策** — 当用户给出一个文件或内容描述时，判断它应该放在哪个分类
2. **命名检查** — 检查文件名是否符合 `YYYYMMDD-描述.扩展名` 规范，不符合的给出建议
3. **同步建议** — 根据内容敏感度，建议应该同步到哪些设备
4. **架构合规** — 新增内容时确保不破坏现有的十槽分类结构

## 分类决策流程

收到一个文件或内容描述时，按以下顺序判断归属：

```
它是官方证件/证书/党员材料？
  → 00-个人档案

它是本科/硕士阶段的学术成果（论文、课程项目、竞赛）？
  → 01-教育经历

它是当前/过去工作相关的内容？
  → 02-工作经历（然后在下面按公司分）

它是各类笔记：环境配置、编程、AI-ML、论文研究、工作日志等？
  → 03-笔记空间

它是自己写的文章、博客、反思？（非商业）
  → 04-思考创作

它是论文/学术参考资料？
  → 05-参考资料/02-论文（用子目录关联工作上下文，如 论文/时序布局/、论文/LeDSE/）

它是书籍/工具手册？
  → 05-参考资料（按子类分）

它是医疗相关的（体检报告、病历、保险）？
  → 06-健康医疗

它是财务相关的（投资、保单、税务）？
  → 07-财务管理

它是生活体验记录（照片、旅行、日常）？
  → 08-生活记录

它是个人项目、独立作品？
  → 09-个人项目

它是大于 50MB 的大文件？
  → 99-大型文件（存储层，不参与同步，在分类目录放 .link.md 占位）
```

## 命名规范

### 文件（自己产出的）
```
YYYYMMDD-描述[-v版本].扩展名
```

### 文件（下载的参考资料）
根据资料实际标题命名，不保留下载时的随机文件名。优先从 PDF 元数据或正文提取标题。
示例：`SykdfdRlrLPEQBBX.pdf` → `PrimeTime User Guide - v2016.06.pdf`

### 文件（官方/代码）
保留原名，不重命名。官方文件有法律效力，原名是身份的一部分。代码遵循项目自身命名规范。

### 目录
- 有逻辑顺序的：`数字前缀-名称`，如 `01-方向A/`
- 独立容器的：直接用名称，如 `项目A/`
- 日期分组的：`YYYY-MM`，如 `2023-11/`

## 同步建议

| 分类 | 同步策略 |
|------|----------|
| 00-个人档案 | 不同步，仅本地 |
| 01-教育经历 | 全设备（只读归档） |
| 02-工作经历 | 公司+家里+服务器 |
| 03-笔记空间 | 全设备 |
| 04-思考创作 | 全设备 |
| 05-参考资料 | 全设备（视频仅家里+服务器） |
| 06-健康医疗 | 不同步，仅本地 |
| 07-财务管理 | 不同步，仅本地 |
| 08-生活记录 | 全设备 |
| 09-个人项目 | 按项目决定 |

## 输出格式

当用户询问文件归属时，回复结构：

1. **建议分类路径**：`XX-分类名/子目录/文件名`
2. **建议命名**（如果原名不符合规范）
3. **同步建议**：应同步到哪些设备
4. **备注**：任何需要注意的边界情况

## 维护规则

**提交前检查同步。** 当用户明确表示要提交/同步当前修改时，检查改动是否涉及以下内容：

- 十槽分类体系（目录结构、命名）
- 文件命名规范
- 同步策略
- 跨平台映射规则

如果涉及上述内容，**必须先提示用户确认**再执行，提示语：

> 本次修改涉及 [分类体系/命名规范/同步策略]，是否需要同步更新 README、Skill 并提交到服务器？

用户确认后，按以下顺序执行：

```
1. 更新 个人管理系统/README.md
2. cp 到 references/personal-management-system.md，两份保持一致
3. 同步到 SKILL.md（如涉及规则变更）
4. cd ~/.claude/skills/personal-management-system && git add -A && git commit -m "描述" && git push
5. 询问用户服务器地址和目标路径后，执行 rsync 同步
```

**反向同理**：修改 skill 规则时，也要提示是否同步回 README 和服务器。

## 权威参考

完整的分类架构、命名规范、同步策略、跨平台映射等详细内容，参考 `references/personal-management-system.md`。
当有歧义或需要确认细则时，优先查阅该参考文件。
